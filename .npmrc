# HYEHOST & Low-Memory VPS Configuration for npm
# Drastically reduces RAM usage to prevent OOM "Killed" during installation

audit=false
fund=false
progress=false
prefer-offline=true
maxsockets=1
package-lock=true
omit=dev
omit=optional
ignore-scripts=true
loglevel=warn

legacy-peer-deps=true
